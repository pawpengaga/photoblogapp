<%= render @publication %>

<div class="mt-3">
  <%= link_to "Volver", publications_path, class: "fw-bold" %>
  <% if user_signed_in? %>
  <% if current_user.id == @publication.user_id %>
  <%= link_to "Editar", edit_publication_path(@publication), class:"ms-3 fw-bold" %>
  <%= button_to "Eliminar publicación", @publication, method: :delete, class: "btn mybtn mt-5" %>
  <% end %>
  <% end %>
</div>

<%= render 'comments/form' %>



<div class="my-4">
  <% @comments.reverse.each do |comment| %>
    <div class="mb-3"><%= render partial: "comments/comment", locals: { comment: comment } %></div>
  <% end %>
</div>