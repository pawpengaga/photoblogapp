<%= form_with(model: publication) do |form| %>
  <% if publication.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(publication.errors.count, "error") %> prohibited this publication from being saved:</h2>

      <ul>
        <% publication.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row d-flex justify-content-center mt-4">
    <div class="col-8">
      <div class="mb-3">
        <%= form.label :title, "Título", style: "display: block", class:"fw-bold mb-2" %>
        <%= form.text_field :title, class:"form-control" %>
      </div>



      <div class="mb-3">
        <%= form.label :description, "Mis pensamientos", style: "display: block", class:"fw-bold mb-2" %>
        <%= form.text_area :description, size: "120x4", class:"form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :images, "Imagen adjunta", style: "display: block", class:"fw-bold mb-2" %>
        <%= form.file_field :images, multiple: true, class: "form-control" %>
      </div>
      <div class="d-flex justify-content-end">
        <%= form.submit "Subir", class:"btn mybtn fw-bold" %>
      </div>
    </div>
  </div>
<% end %>
